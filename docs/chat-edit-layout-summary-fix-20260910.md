# 编辑发送布局与历史摘要排查（2026-09-10）

## 页面回归：已复现并修复

新增编辑功能后，短消息的复制/编辑/取消检查没有覆盖长历史编辑后发送。用户补充了准确触发条件后，使用隔离 QA 画布、24 条模拟历史、编辑第 13 条消息、模拟延迟流式回复重现。

- 旧生产版本：发送前 root.scrollTop=0、canvas.top=0；发送后 root.scrollTop=405、canvas.top=-405。文档 scrollTop 和根矩形 top 均为 0，所以仅检查文档滚动或根高度会漏检。
- 原因：消息尾部 `scrollIntoView` 滚动了包括 `overflow-hidden` 页面根在内的祖先容器，画布与聊天栏内容一起偏移。
- 修复：聊天自动滚动只调用消息列表自身的 `scrollTo`；编辑框聚焦使用 `preventScroll`。`100dvh` 只是额外视口适配，不是此故障的直接修复。
- 新生产版本 `.next-production-chat-scroll-fix` 在同一流程中通过：根滚动位置和画布位置均保持不变。覆盖同页 900→668→850→900 高度变化、复制、长历史编辑与延迟回复完成。
- 前端 48 项相关测试、TypeScript、生产构建通过。浏览器测试模拟消息保存与 Agent 流式返回，不提交真实生成任务，不改用户画布。

截图：`artifacts/message-actions/edit-send-complete.png`。

## 摘要故障：独立问题

真实失败记录为 `a6cf368e-ef64-4e2f-859f-44c9b1796a60`，错误 `agent_context_summary_failed`，模型使用会话冻结的 `deepseek-v4-flash-vision-exp`。不会通过页面修复宣称摘要问题解决，也不会自动重放用户生图请求。

隔离探针复现：相同供应商 `toapis.cn/v1`、精确模型、6 条历史 process 记录，正文长度 0，finish_reason=length，1,500 个输出 token 全部用于推理。不是输入上下文溢出，也不是模型永久不支持摘要。

修复：独立摘要调用的总输出上限改为 `min(6000, 已有 generationReserveTokens)`，既为推理保留空间，也不突破现有安全预算；主模型、意图检查等其他调用未改变。对截断、空正文、无效格式和来源不匹配增加静态分类诊断，仍拒绝不完整/错误摘要，仍不自动重发。

修复后的同模型隔离调用通过：正文 1,311 字符、finish_reason=stop，实际 input=2,603、output=1,791（其中 reasoning=1,154）；JSON、schema、来源 ID 校验全部通过，0 个工具调用。该验证读取旧 checkpoint，但不写用户会话、不调用生图，不等于完整生图链路重新验收。

服务端上下文/摘要/模型预算/110轮历史集成回归 42 项通过。修复已加载到本地 API，健康检查 HTTP 200。历史错误消息保留为记录，用户下一次主动发送使用新逻辑。

## 实际分工

Terra/medium 排查页面并构造精确浏览器复现；Sol/high 定位摘要推理额度耗尽；Astra 主控接手最小摘要修复、回归、真实摘要复验及部署。摘要补丁收尾由主控执行，没有把代理诊断当作已实施。
