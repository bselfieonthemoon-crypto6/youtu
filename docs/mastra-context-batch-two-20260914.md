# 第二批：Auto 与失败恢复（2026-09-14）

状态：代码回归完成，真实浏览器生图验收因本地数据库故障未执行，尚不算第二批完整验收通过。第一批上下文实现未修改。

## 修复

### Auto 来源绑定后的重新选择

`mastra-image-tool.ts`：新图最初按无参考图完成 Auto 选择后，来源判断可能发现其实际需要参考图。此前 reference 分支传递已解析的 `ratio.args.model`，把自动选择误当明确指定，阻止按新增来源重新选择。

重新解析时保留原始 `raw.model` 意图，而不是第一次 Auto 结果；手动 UI/明确模型继续优先。未增加渠道、模型或调整已有模型能力优先策略。先加入失败测试（Auto 错选 workspace:auto），修复后同场景通过。

### 终态回执分类

`mastra-image-jobs.ts` 原终态重放只返回 jobId/error，丢失持久 error_code。现在返回 errorCode/retryEligible，扩展 `SubmitImageJobFn` 可选返回字段，工具保持透传。

retryEligible 采用当前 finalizer 的既有口径：仅 dead_letter + provider_rejected 为 true；不是自动重试许可。unknown、canceled、其他失败仍为 false。不改变用户后续正常对话或明确新请求的授权，不新增跨轮固定句式拦截。

## 测试结果

- 14 个测试文件、182 项通过；服务端 `tsc --noEmit` 通过。
- 包含第一批上下文/Memory/来源复用回归、Auto 后发现参考图、手动模型保持、明确或自动绑定编辑路径的 UI 比例优先、错误分类完整透传、终态重放不新建任务。
- 故障隔离测试覆盖同模型兼容渠道明确拒绝后的回退、全部拒绝、未知结果停止、取消、持久 checkpoint、终态卡片。为模拟传输/隔离测试，不是本轮真实第三方故障。
- 1000 轮用例仍为合成上下文测试；本轮实际模型请求 0 次，未将上一批的真实成功计入本轮。

## 真实验收阻塞与下一步

启动验收前查询本地数据库失败。容器 `supabase_db_thtdhcvjppuvlvahfmga` inspect 显示 exited、ExitCode 139、OOMKilled false、FinishedAt 2026-09-13T17:52:44.356597129Z，错误包含 Docker 底层 hosts 文件 `read-only file system`。尝试只启动该既有容器仍报相同错误。docker ps 的旧健康状态与 inspect/exec 不一致，不能按 ps 单独判定可用。

没有删除/重建容器、卷或数据库，没有重启全局 Docker/WSL，没有切换远程数据库，也没有重启 API/worker。上述基础设施恢复可能影响其他项目，应先由用户恢复或明确授权处理。新代码尚未通过应用重启加载。

恢复后：先核验数据库/认证健康与无活动任务，加载 API 新代码；在独立 QA 会话验证 Auto 新图→引用上一张图做同系列→手动模型和比例→失败后继续对话/明确重新生成。不得动用户当前会话。

## 仍未覆盖

- 本轮真实 Auto、真实付费生成与失败后恢复。
- 真实所有渠道同时故障、多租户并发、扣费/退款。既有回退仍是同 upstream 的兼容连接，不是任意跨模型替换。
- 跨轮结果未知时的意图判断仍依赖当前状态上下文与语义规则；本次没有为了这一风险增加“继续/重试”固定句式硬拦截，不能宣称所有误判已经消除。

分工：Terra / gpt-5.6-terra（medium）审查失败恢复并修复终态回执；主控复现/修复 Auto、补类型与工具贯通测试、完成整合回归。没有引入无人值守、第二套上下文或额外框架。
