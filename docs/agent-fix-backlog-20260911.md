# Agent 待修复与验收清单

状态：本次集成修复及 6 轮真实对话验收完成，详见 integrated-agent-stability-20260911.md。未勾选项仍是验收缺口，不代表全场景完成。

- [x] P1 确认与方案关联：自然确认、修改后的确认、失败后同会话恢复已验证；最近 Logo 来源指代漏判亦已修复。每次真实确认仅创建一个任务，并发场景仍见下方缺口。
- [x] P1 审核可靠性：截断限定一次输出预算恢复，仍受上限及总超时约束；内部失败不归因于用户需求不清。可控回归通过。
- [x] P1 真实状态反馈：区分本次写入失败、既存方案、任务提交与成功结果；失败不删除旧方案，也不恢复过时授权。真实结果已核对写入画布。
- [x] P1 取消竞态：新的备用供应商、后处理及分层推理之前重新检查取消状态，可控回归通过；已被第三方接收的请求不承诺可撤销。
- [ ] 验收缺口：并发确认、非零扣费退款、多用户隔离、带图片的重启与压缩恢复、真实多参考图。
- [ ] P2 清理无人值守死代码及遗留执行权限：遗留执行权限已关闭并核实；未进行所有历史死代码的全量删除，保留审计数据。

## 新增：Skills 调用与实际生效审查

用户反馈：多轮对话中没有明显看到技能调用。暂不能归类为“完全未调用”。

当前证据：artifacts/paid-dialogue-live/qa-production-series-20260911.json 为隔离测试会话 11e0af83-c478-424b-98ac-da9223aecacc，不是用户当前浏览器会话。

- 第 2 轮 Logo 方案：list_skills、compose_skills、generate_image。
- 第 4 轮同系列海报：list_skills、use_skill、inspect_canvas、generate_image。
- 第 6 轮局部修改：get_image_proposal、inspect_canvas、generate_image，没有新的技能调用。

这些工具名证明发生过技能相关调用，但单凭记录不足以证明加载了哪个完整技能、是否注入最终模型请求、后续压缩是否保留必要约束以及模型是否实际遵循。

- [x] 核对用户当前会话的技能选择与调用：实际历史证据见下方；新实现使用独立会话验收，不改写用户会话。
- [x] 追踪 list/compose/use 的参数、结果、注入位置及最终模型请求；集成测试验证完整正文恰好一份，不把技能枚举视为使用。
- [x] 检查新类型任务切换、同品牌续作和局部修改；压缩保留当前指南经过可控测试，真实超长会话仍需专项验证。
- [x] 技能回执显示实际加载的业务指南名称；浏览器实际显示 campaign-design，加载不冒充生成成功。
- [x] 补充完整最终请求、压缩、阶段替换、预算及真实文字修改/海报约束回归；不承诺模型永远完全遵循。

### 进一步核对的业务技能证据

查询 tool_executions 的实际参数与结果后确认：

- 用户当前会话 1422b92f-400f-4dc7-b400-6151ba0ee76b 调用了 use_skill(name=logo-design)，不是只有枚举。
- 隔离测试第 2 轮 compose_skills 的 primary=logo-design，helpers=[typography-layout,json-image-prompt]，返回 status=composed。
- 隔离测试第 4 轮宣传海报加载 canvas-design，没有观察到更具体宣传图技能调用；需要检查与纯生图交付类型是否匹配。
- 两个会话均没有 search_prompt_library 或 get_prompt_library_entry 的持久化调用。不能声称这些图片使用了提示库检索。
- design-guidance.ts 当前按 relevant visual reference needs 提示模型自行选择提示库检索，不是每次生图必经步骤。应测试明确要求检索提示库、需要风格探索、已有明确参考图的不同场景；不要机械地每轮重复检索。

以上证明已加载业务技能与提示库未检索的边界，不证明模型完全遵循了技能正文。
