# 第一批：保守上下文优化（2026-09-14）

## 边界

稳定性优先。不缩短对话窗口，不减少参考图数量，不更改历史摘要策略、最新需求、文字原文、比例或来源绑定规则。不修改 Auto、渠道回退、视频、任务卡或无人值守架构。

## 实施

1. `mastra-agent.ts` 的工具发现目录只重复列出尚未常驻的工具。常驻工具仍携带完整 schema 与说明，可直接调用；非驻留工具仍保留名称和简述并可按需加载。
2. `mastra-image-source-grounding.ts` 的复用范围仅限同一 grounder 内、相同用户/工作区/会话/画布/执行请求/方案/来源清单的已验证语义判定，最多 8 项 LRU。不是跨轮记忆，不缓存异常或图片二进制。每次重新读取来源和校验可用性，不复用旧授权结果。
3. 原先完整 grounding 结果的复用改为仅复用语义判定，避免来源权限或可用性变化被旧结果掩盖。并发相同调用不合并，可能重复判断，以免一个调用的取消影响另一个。

## 验证

- 10 个文件、104 项代码/SDK 回归测试通过；其中 1000 轮上下文用例是合成测试，不是 1000 次真实模型请求。
- 服务端 TypeScript 检查通过。
- 新增/强化：常驻说明不重复、延期工具下轮可调用、相同语义判定复用但每次重新读取图片、访问撤回后失败且恢复后可继续、判定失败不缓存、不同 run 不复用、8 项有界淘汰。
- 本轮真实长历史回忆：`e0340e09-3925-4dea-b51c-7c40065264e0`。正确回忆潮汐织所、MAKE ROOM FOR LIGHT、#6B4EFF、2:3、11月23日、南岸店，无写入工具。证据：`artifacts/paid-dialogue-live/memory-acceptance-2026-09-13T17-45-07-128Z.json`。
- 本轮真实连续改图：run `7a7d656d-b0b8-47b1-8661-8ceb7fd11fc6`，job `3d9e4280-23f5-4439-a8bc-56ab4b5d5ee9` 已 succeeded。调用 edit_image、携带 1 张来源图 `2c86f29e-ee1c-4c7c-9b89-eee696cbe4cf`、请求比例 2:3，提示中六项确认信息完整。产物 `c9d22258-9ec6-4ec2-869e-554faa856173`，1360×2048（渠道尺寸量化，并非数学精确 2:3），provider_attempt_count=1，无 fallback。聊天图片解码且画布无需刷新交付；截图确认拱形光晕和关键文案。证据：`artifacts/paid-dialogue-live/browser-turns/2026-09-13T17-46-31-364Z.json` 和同时间戳 `live-delivery.png`。
- 该真实改图走明确 asset ID 路径，不作为语义判定缓存命中率证明；缓存命中/失效边界由定向测试覆盖。采集器中的重复事件不是第二次生成：数据库仅有一个对应 job，界面一张结果卡。
- 结束时全局 active agent_runs/background_jobs 均为 0；本地 API PID 4120，Mastra + observational Memory，未进入下一批。

没有测量或承诺实际 token 节省比例；这次只消除有明确重复证据的说明和收紧复用边界。测试使用独立 QA 会话，不修改用户当前会话。不据此宣称无限轮零丢失、全渠道或多租户并发验收完成。

分工：Terra / gpt-5.6-terra（medium）负责来源判断复用与局部测试；主控负责工具目录、整合回归和真实浏览器验收。
