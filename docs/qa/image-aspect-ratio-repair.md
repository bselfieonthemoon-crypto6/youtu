# 图片显示比例修复

对话生成结果卡片取消 aspect-square、object-cover 和悬停缩放，使用图片自然高度、最大高度约束及 object-contain。以文件本身的比例展示完整图像，不依赖可能错误的旧 artifact 尺寸。侧栏预览与画布的像素大小仍可不同。

当前画布 0560072c-5c8a-4954-b037-9b476246a671 的任务 6aaae7f6-fd96-4659-bece-89e89c527ceb 已通过下载原文件核对尺寸 1536×1024。事务中将对应节点从 600×600 改为 600×400，保留坐标、角度及 scale，递增节点 version 和画布 revision；同步任务和结果消息尺寸。未修改图片文件、未重新生成、未重置其他画布元素。

原任务、画布和结果消息已备份到受保护的 loomic_backup_20260906.image_ratio_repairs，键为该 job_id。此备份不是全库离线备份。恢复时应按字段处理，避免覆盖用户后续编辑。代码回退不会自动撤销本次数据修复。

组件回归测试覆盖完整显示、不再方形裁切及不再 hover 缩放；既有对话计划链接测试通过。修复后重新查询任务尺寸与节点比例一致。已打开的页面需刷新以读取修正后的历史画布。
