# 真实对话验收覆盖审计

> 范围已变更：不再验收无人值守、自动推进与自动检查。普通多轮对话和最小执行可靠性仍须验证，以 `conversation-first-reliability.md` 为准。

本文件对照 `agent-paid-dialogue-matrix-20260910.md`，区分“行为类别已有实测”与“原示例的具体格式/措辞已实测”。失败轨迹不删除，不用测试通过替代真实任务。下列 ID 可在验收记录、browser-turns 原始轨迹或本地 replica 中核查。

| 用例 | 当前证据与结论 | 未完成项 |
|---|---|---|
| A1 模糊 Logo 澄清 | `db561694` 未调用生图，询问品牌需求 | 已覆盖核心边界，不要求一次机械询问所有字段 |
| A2 补充品牌但尚未确认 | Northstar 明确完整 brief 后保存，确认前无 job | 原示例“仍有信息缺口”的中间轮未单独核验 |
| A3 两种交付物冻结 | 单张方案生命周期实测多次通过 | 透明图+横版组合的持久计划未实测 |
| A4 确认后执行 | `d553d0a8` 一次确认 job `81327087`，成功交付 | 具体示例措辞未覆盖；使用明确“确认生成” |
| A5 重复确认 | `67da8b9a`（生成中）、`8dd6765e`（完成后）复用原 job | 已覆盖行为类别 |
| A6 查询真实状态 | 旧失败保留；修复后 `11a2f7e2` 直接读取同会话会员卡 job `08f135e9` 的 succeeded，tools=[]，无新增 job，回复已持久保存 | 明确状态查询已通过；更广泛自然措辞仍由通用 Agent 处理，未宣称全部覆盖 |
| A7 同系列下一物料 | `26a047b9` → `d553d0a8` → 横版 `81327087` 完成 | 已覆盖从旧品牌继续的新物料 |
| A8 不再做待确认物料 | `da98f2c6` 具名取消，proposal `3c189a25` canceled / job=0 | “先别做横版”的自然措辞未单独覆盖 |
| B1/B2 改用途并确认 | Northstar Logo→4:5 宣传图 `43eaa6da`，真实原图 asset/hash；River Club 16:9 完成 | 具体 3:1 邮件头图未实测 |
| B3/B4 局部修改 | `bafcd6ec` / `04cd44a5` → `98aa1da2`，真实原海报参考、按钮蓝改紫、旧图保留 | 非编辑区域为视觉验收，不保证逐像素一致 |
| B5 刷新恢复 | Alpine Escape 关闭浏览器后完成，刷新成功；失败及取消刷新均持久 | 生成中刷新后“自然语言继续”未单独跑 |
| B6 竖版续做 | Alpine Escape 横版→9:16，job `67aa9105`，原图引用与比例通过 | 原示例 3:1→4:5 未逐字重复 |
| B7 短答授权 | 修复后 `9b62669f` 短答不执行、不重做；`d6316d9b` 明确确认原方案成功 | 已通过当前短答与后续明确确认的链路 |
| B8 多图含糊指代 | 修复后 `f316c5a3` 实际页面收到询问图片与部位的澄清，无工具/无生成 | 当前裸指代改色场景通过；不宣称所有歧义语句已覆盖 |
| C1 纯讨论 | `cf03ec21`、`d9cb4aa8` 无工具/无生图 | 已覆盖 |
| C2 讨论转制作 | `679f9fa1` 保存会员卡方案 `08f135e9`，pending / job=0，未直接收费 | 已覆盖讨论转待确认方案；回复存在重复解释 |
| C3 精确方案确认 | C2 会员卡 `08f135e9` 经 `d6316d9b` 确认成功，asset `ca43c666`；1254² | 执行链路通过；含装饰小字，不宣称严格限定文字的视觉质量通过 |
| C4 三种格式串行 | `05e91bef` 仅文字清单；`889b1d11` 发现 write_todos 未注册及旧图来源核对失败。已修注册，本地 harness checkpoint 回归通过 | 修改尚待部署和真实复测；透明通道、JPG、串行接续均未验收 |
| C5/C6 构图参考但排除旧 Logo | 已有真实参考图链路 | 排除参考图 Logo 的专门请求尚未核验 |
| C7 供应商真实失败 | `4771cbd9` HTTP400、dead_letter、attempt=1；前端与画布显示失败 | 非人为造故障；未出现真实 timeout，不冒充超时测试 |
| C8 生成中取消 | `60558040` 实际 running 后取消API，canceled；供应商后返回未发布、未重试 | API 已覆盖；自然语言取消正在生成仍待补，不与待确认方案取消混淆 |
| C9 失败后新尝试 | 旧 `4771cbd9` 保持终态，重新保存/确认 `67aa9105` 后成功 | 已覆盖新确认尝试；不声称原失败 job 被恢复 |

## 额外真实故障修复

- 自动检查：图片完成→绑定→自动触发→查看像素→保存结论已实测；确认误停接续、四轮预算浪费、正确项被当 blocker 均已定位修复。
- 手动检查最近结果：`e0e01522` 只查看最新真实 asset，一次检查，输出与检查一致；不复制旧验收到新任务。
- 画布显示比例：最新 `81327087` 原图 1672×941、元素 511.7279489904357×288，真实页面符合；旧错误样例保留。
- 同系列请求契约：前端错误携带 sessionId 导致 invalid_command，已修复并真实重新提交成功。

尚不能把整份矩阵标为全绿。后续按“未完成项”补测，优先复用现有隔离画布和图片，不重复已覆盖的付费调用。
