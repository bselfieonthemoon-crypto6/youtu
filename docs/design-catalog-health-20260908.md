# 四类设计资源完整检查（2026-09-08）

## 结论

初次审计发现 6 套模板的 8 个文字对象缺少括号字形，会使整张预览失败。主流程浏览器验证通过。用户随后明确要求删除问题资源，已通过正常管理接口软删除这 6 套模板；未替换字体或修改生产功能代码。下表为删除前审计，删除后结果见末节。

## 实际资源检查

| 模块 | 数量 | 结果 |
| --- | ---: | --- |
| 模板 | 147 | 结构校验全部通过；逐套实际后台渲染 141 成功、6 失败 |
| 素材 | 3965 | 目录全量分页、关联存储记录通过；按类型均匀抽取 48 个文件实际下载/解码通过 |
| 文字预设 | 9 | 结构和全部绑定字体文字渲染通过 |
| 字体 | 14 | 文件全部下载、fontkit 解码及当前用户内容 API 读取通过；不等于每个字体覆盖任意字符 |

共检查 676 个模板/预设文字对象、3979 个底层资产关联；当前用户读取四类目录的数量与数据库范围一致，未发现游标循环/分页重复。模板、文字、字体的工作区资源处于 pending_review，但当前工作区成员可读；这是现有目录权限设计，不是资源丢失。

## 已按用户要求移除的资源问题

| 模板 | 失败文字对象数 |
| --- | ---: |
| 解读 | 1 |
| 美食 | 1 |
| 招聘会 | 1 |
| 新闻 | 2 |
| 宠物 | 2 |
| 蛋黄派 | 1 |

绑定字体是 HappyZcool-2016（face 9d816ae7-559e-44b1-b487-a106bd2b1157）。实际失败文本包含 U+FF08 全角左括号。独立读取字体文件进一步确认：全角左右括号以及半角左右括号均无字形，cmap 和 layout 都返回 glyph 0。这不是已修复的 CR 换行误报。

未找到完整同名字体，且用户要求模板保持原字体。用户选择移除问题模板，因此没有修改文字或替换字体。新输入的字符仍有字体覆盖限制，不能把此次目录清理理解为任何文字都可以使用任何字体。

## 自动化验证

- 后端资源导入、目录读取、资源/模板管理、字体、预览渲染：61 项通过。
- 前端资源面板、字体加载、模板弹窗、Fabric、编辑会话：25 项通过、2 项失败（另有 1 个关联未处理拒绝）。没有把失败统计为通过。
- 模板绑定用例还在查找旧文案“智能替换 · 活动模板”，当前界面已改为“替换模板变量 · 活动模板”。
- 大尺寸导出用例进入了浏览器导出分支，jsdom 缺少 URL.createObjectURL，发生未处理拒绝。测试共享的 serializeScene mock 仍返回普通尺寸场景，需校正测试隔离/桩数据后才能给该项结论；不能据此直接断言浏览器实际导出损坏。
- 真实 Chromium 用例 inline-artboard-live.spec.ts 通过（35.6 秒）：素材拖入并校验坐标和资产 ID、文字预设插入、字体应用、不同尺寸模板替换、撤销/重做、保存及刷新恢复。
- 浏览器测试仅创建隔离项目 480097e2-67e9-4753-a74c-329f87d0a861，没有在用户现有画板上做替换测试，也没有新增已发布的目录测试素材。

## 边界

没有逐张下载并人工比对全部 3965 张素材；存储记录存在不等于每张二进制均已验证。147 套模板全部执行后台渲染，但没有逐套与浏览器进行像素一致性比较。新输入的任意字符仍取决于字体覆盖范围；本报告不承诺未来零故障。

复测：在 apps/server 中使用本地 app.env 执行 scripts/audit-design-catalog-health.ts --full-preview。审计脚本采用只读数据库事务，输出 artifacts/design-catalog-health-20260908.json。

## 用户授权的清理

最新状态：用户随后明确要求不保留，已执行 scripts/purge-broken-local-templates.ts，在事务中永久删除上述 6 条模板和其专属资产/字体/标签关联。确认没有 Agent 请求外键引用，且设计文档、共用字体、素材与底层资产全表指纹不变。本次产生的两份 deleted-broken-templates 备份 JSON 已删除，不能再通过目录 restore 恢复。以下软删除记录仅为先前操作历史。

- 使用 scripts/remove-broken-local-templates.ts，仅允许本地副本，逐一验证审计失败 ID、名称、工作区与未删除状态。
- 通过 POST /api/admin/design-catalog/delete 执行版本校验、权限校验和可恢复软删除，六项均成功，revision 从 2 变为 3。没有物理删除文件。
- 完整原始行备份：artifacts/deleted-broken-templates-1788865893602.json。恢复可走现有目录 restore 接口；该接口按现有规则恢复为 draft。
- 删除前后全表指纹一致：design_documents、font_faces、design_resources、asset_objects；现有画板和共用资产没有被此次操作改动。
- 删除后完整复测：141 套模板实际渲染全部成功；9 个文字预设、14 个字体、3965 条素材目录及 3979 条资产关联检查通过，48 个素材二进制抽样通过，报告 issues 为空。当前用户 API 分页返回 141 套模板，与未删除数据库记录数一致。
