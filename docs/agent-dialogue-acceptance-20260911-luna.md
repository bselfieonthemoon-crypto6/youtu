# 对话式 Agent 真实验收（Luna + 主控）

## 本轮最终结论

主链已实际跑通，但不能据此宣称所有模型和所有边界都稳定。本轮新增 **5 张真实成功图片、1 次真实供应商比例失败**；历史恢复画布原有的 2 张图片不计入新增。最后两会话共 7 个成功 job、1 个失败 job，无遗留运行中 agent run。

| 验收项 | 结论 | 真实证据 |
|---|---|---|
| 信息不足先追问→补齐品牌要求→确认首图 | 通过 | 首问 `a64e3c3b...` 为真实 API/CLI，不是浏览器；后续浏览器补充需求与确认，Luna Logo `54fb13f3...` 成功 |
| 同系列继续生成，保留参考图/品牌/文案 | 通过（gpt-image-2） | 同一会话 `00bc5a73...` 成功，刷新后真实海报解码并显示 |
| 连续改标题→改色→再改标题 | 通过 | `c7d1f5dc...` 晚晴、`285853aa...` 暖橙晚晴、`c6954033...` 清露全部真实成功 |
| 修改要求与确认放同一消息 | 修复后通过 | run `eee31b21-7570-49f6-81bc-4cf508f8b23c` 一轮冻结并提交，未二次追问；清露 asset `ba0a87eb-ff2a-4670-ae9a-8874cf5a032c` 已交付 |
| 生成中/已完成重复确认 | 通过 | 返回同 job，未新建重复图片；完成后自然重试 run `ff82f8dd...` 与确认 `f7f2c8c0...` 仍为清露原 job |
| 第三方失败后同一会话换模型恢复 | 通过 | nano 失败后切换 gpt-image-2，山岚成功；并非后台伪造结果 |
| 取消待确认方案 | 修复后通过 | 原话重跑 `18cbd814-2d5d-4cf1-b59c-17b879205561` 工具 canceled，DB `432339da...` canceled，零新 job |
| 取消后继续聊天/纯讨论不执行 | 通过 | `ace9d5b2-afda-46a6-ac00-d8ab9e229407` 保留品牌与禁忌，区别已生成与已取消方案，tools=[] |
| nano-banana-2 非方形交付 | 失败，未修复该供应商适配 | 4:5 返回 1024×1024，被阻止发布；不能拿换 GPT 成功冒充 Nano 通过 |
| 明确失败后原模型自然重试到交付 | 未完整覆盖 | 命名重试 TS/SQL 已修并通过守卫测试，真实已完成结果的复用已测；原失败终态的新尝试未重跑到交付 |
| 多租户并发、断网中断恢复、多参考图、数百轮长对话、全部第三方错误类别 | 本轮未覆盖 | 不以旧报告/单元测试代替这些真实场景 |

新增图的 1632×2048 属于项目 4:5 容差内输出，不是数学上严格 4:5；生成式编辑也不保证未修改区域像素完全一致。历史 reviewer schema_invalid 曾真实出现，后续修复场景正常，尚未经过高次数稳定性统计，不宣称这类偶发已彻底消失。

模型分工：实际 **gpt-5.6-luna / medium** 跑真实对话；**gpt-5.6-sol / high** 复核并修复跨 TS/SQL 确认边界；主控定位、集成、执行迁移与独立浏览器复核。应用文字模型为 deepseek-v4-flash-vision-exp；实际生图使用 nano-banana-2 和 gpt-image-2。辅助回归/类型检查通过，但不计作真实对话轮数。

本地迁移 14/15/16 已实际执行及登记；最终 API PID 39152、Web 17844、网关 28044、Worker 18888。未修改用户原画布或开启无人值守。

最后交付截图：`artifacts/paid-dialogue-browser/51deede3-b8b6-4a19-8a53-78ed36b4e7b3-qinglu-delivered.png`。以下保留过程记录，早期“等待/失败”由本节最终状态覆盖，不删除失败证据。

## 验收口径

由实际 gpt-5.6-luna / medium 子代理执行新用户正常流程，主控复核历史失败恢复、修复源码及前端交付。不以 run.completed、工具成功或单元测试通过代替图片交付。通过真实页面发送，真实第三方模型和生图，真实数据库与 worker；不直接伪造 proposal/job。保留失败轨迹，不改用户当前画布。

测试画布：

- Luna：`5b23970f-96d2-4113-8b16-5c0f21ddbf4a`；session `6a43173e-a0e5-483b-9072-2425f5b21e90`；manifest `artifacts/paid-dialogue-live/qa-luna-real-series-20260911.json`。
- 历史失败恢复：`51deede3-b8b6-4a19-8a53-78ed36b4e7b3`；session `11e0af83-c478-424b-98ac-da9223aecacc`；manifest `artifacts/paid-dialogue-live/qa-production-series-20260911.json`。
- 浏览器每轮详细轨迹：`artifacts/paid-dialogue-live/browser-turns/`。UI 驱动不回写 manifest.turns，因此轮数必须结合这些轨迹与会话数据库核验。

## 已复现并修复的阻塞

1. 历史修改恢复 run `09fd2c85-5cc2-4ac8-a126-d6880d35450f`：意图 reviewer 两次截断（finishReason=length），没有提交图片；并非第三方生图拒绝。只读 DeepSeek V4 reviewer clone 改为非思考 JSON 输出，主对话不变，仍严格校验完整 JSON、原话引用、权限和真实对象范围。[DeepSeek 官方思考模式](https://api-docs.deepseek.com/guides/thinking_mode/)、[JSON 输出](https://api-docs.deepseek.com/guides/json_mode/)提供参数依据；第三方兼容性以随后真实运行结果为准。
2. 下一轮 run `ed6380dc-e580-40fb-a6a1-fed6f40b02e9` 及 Luna run `e36094d1-f303-4f83-850d-4f7c2506b332`：截断问题消失，但旧 selector 要求时间词和图片名词同时出现，拒绝了“沿用刚才澄屿 CHENGDAO 的叶片与水波”和“继续刚才未成功的修改：把山岚改成晚晴”。修复仅规范化独立新图的只读来源：当前原话唯一匹配真实成图名称/既有文案，而且实际 inputImages 与该存活资产严格一致才放行；不扩张原图写入或付费确认权限。
3. 既有文案来自当前用户/会话/画布成功任务对应的冻结方案，不从模型自报、可改画布 customData 推断。来源文案只加载最近最多 10 个存活来源、每个最多 8 个短标签，避免复制整份历史 prompt。重名、错误来源和越界对象保持拒绝。

辅助回归：reviewer 相关 158 项通过；来源定位修复后 gate/context/provenance 165 项通过，包含实际两种措辞、重名与错误来源。它们不是端到端通过依据。

## 前端证据

Luna 首图 job `54fb13f3-fb45-4195-8789-c32dbcf545da`、asset `a9697072-ad26-44a8-8e0e-a2315a1c86d4`：真实 1024×1024 图已生成。主控独立浏览器刷新后图像解码成功、画布存在图片、消息输入框可见、无 pageerror，并查看截图确认显示。

证据：`artifacts/paid-dialogue-browser/5b23970f-96d2-4113-8b16-5c0f21ddbf4a-luna-first-image.json` 及同名 PNG。

## 待最终更新

4. 确认已冻结方案被误当成重新选图：`3f323b19-2698-409c-a153-104e1cc239bf` 保存正确晚晴草稿后，因画布有 Logo/海报而被错误要求再指定来源。独立输出确认现使用冻结方案专用只读审核；绑定 confirmationId 并重新验证 sourceAssetIds，仍保留付款、权限和幂等守卫，不覆盖现有画布对象。
5. “确认生成这张山岚海报。”被分类为新需求，导致取不到待确认方案。TS 与数据库迁移 `20260911000014` 一致支持命名确认，并以冻结标题匹配名称；错名、缺少标题、问句和混合改动均不能批准。SQL 在事务中真实执行断言，并已应用本地数据库。
6. reviewer 只引用后半句执行要求时会丢失前半句品牌。来源匹配改为引用同一本轮原始消息全文（最多 2000 字），不读取旧 assistant 文字来补授权；重名及错资产仍拒绝。

再次复测（仍非最终通过）：

- Luna 同系列保存 `a2743e71-52de-47e4-9b34-20e0e184632b`、命名确认 `635ff271-a06f-42cd-a936-686f1b409f46` 已真实提交 job `a8c25835-9853-4855-a981-62739d59f012`。但 nano-banana-2 返回 1024×1024，与 4:5 不符，终态 dead_letter / image_aspect_ratio_mismatch；未发布错误比例资产。这不是交付成功，正在同一会话测试失败恢复。
- 历史晚晴合并“改字+确认”轮 `ef489db8-f356-4013-af67-f2205d378ff4` 的 reviewer 输出 schema_invalid，仍未完全稳定；不能遗漏这个失败。后续自然命名确认 `923afb92-5526-423c-a23f-4b27b85b8b42` 成功提交 gpt-image-2 job `c7d1f5dc-1356-402e-9094-9f9607b3d67e`，正在等待图片。
- 最近一组辅助回归 243 项通过，仅作代码证据，不计真实端到端用例。

本文此时不代表完整验收通过，尚需实际图片交付、连续修改和失败后继续对话的最终证据。

## 续测证据（实时更新）

- 历史修改恢复已交付：job `c7d1f5dc-1356-402e-9094-9f9607b3d67e` succeeded，asset `2c0d76d7-f187-4750-880d-9c62527ea191`，1632×2048。主控浏览器刷新并查看截图：原 Logo、山岚、晚晴均显示，标题确实变成晚晴，输入框可用、无 pageerror。生成式编辑对其余像素不保证完全相同。
- Luna 换模型恢复已交付：原 nano-banana-2 比例失败后，同一会话请求换 gpt-image-2，方案 run `ed2c9949-d3dd-4347-bf40-1e27f837745c`、确认 `f2926220-f101-4e2a-b344-fc0e09ecdeb7`、job `00bc5a73-951d-48e9-8059-ea0672f481d0` succeeded、asset `218a8e88-c97d-4695-82dd-d3356d06e0de`，1632×2048。主控独立刷新查看 `5b23970f-96d2-4113-8b16-5c0f21ddbf4a-luna-series-recovery.png`，失败占位明确显示生成失败，成功海报与原 Logo 可见。
- 纯讨论：Luna run `c4661a6a-fdaa-45b2-a11f-2cde4370e043` 要求讨论系列一致性、不要生成修改，无新生图或画布修改。工具部分返回 unavailable，因此只认证不误执行，不认证视觉审查服务全部可用。
- 确认版本名称：原话“确认生成这张暖橙改色版海报。”曾被单字“改”误杀。Sol 修复授权 TS/SQL，migration15 已实际执行断言、应用并记录迁移；原样复测 run `4738fdde-baa5-4607-90c9-dd0a98e2e5ba` 成功提交 job `285853aa-1933-4bf9-8f47-1b8f225c72bf`。图片尚在等待时不能记交付通过。
- 生成中重复确认：run `3acc4a1c-87f8-40d1-b8a9-0f09fc7ac28a` 再发同句，返回同一个 job `285853aa-1933-4bf9-8f47-1b8f225c72bf`，数据库没有增加另一张任务。
- 测试环境中断：用户暂停后本地网关/API/Web/Worker 进程已停止，DB 会话与图片仍存在。脚本原先忽略 session lookup 的 fetch 错误，误报不存在；已改为先报告 HTTP/transport 错误，并恢复原本地服务。未把此错误计为会话丢失。

尚待处理：“修改要求＋同句确认”曾返回 explicit_confirmation_required；自然失败重试“重新生成，还是刚才确认的山岚海报，比例4:5，其他不变。”已修语法及冻结标题/比例双绑定，但该精确失败场景尚未重跑到交付。供应商 nano-banana-2 的非方形交付仍失败；[该供应商文档](https://docs.apiyi.com/news/nano-banana-2-launch)描述的原生 Gemini 路由与当前 alias/Images 路由不同，未擅自替换用户模型标识，不能宣称该适配已修好。

- 暖橙连续修改已交付：job `285853aa-1933-4bf9-8f47-1b8f225c72bf` succeeded，asset `c02c51b2-df92-4bfa-a300-c408a0a18dec`，1632×2048。主控查看 `51deede3-b8b6-4a19-8a53-78ed36b4e7b3-warm-orange-delivered.png`，四张图（Logo、山岚、晚晴、暖橙晚晴）均显示，颜色改为橙色，主副标题及叶片水波主体保留。刷新后解码成功、无 pageerror。
- 取消复核发现不能误报通过：Luna run `daffe6f7-0c30-4566-9de4-068280f17d3a` 发“先不要生成了，取消刚才的冷泡茶横版方案。”后，get_image_proposal 为 no_current_proposal，但 DB proposal `432339da-39b5-4ff0-9a61-221a2bfbabaf` 仍 pending。根因：命名取消不支持前置停止句/名称与横版顺序，且审核证据只读 current requirement 导致目标被藏掉。已修命名解析及取消专用只读 ID 证据（args={}，不能据此生成），辅助32项通过；须原话复测 DB canceled 后才算通过。

- 最终取消原话复测已落库 canceled；清露 combined 一轮提交后成功，主控实际查看清露截图确认标题、暖橙色及系列构图。随后成功结果复用曾暴露旧英文“尚未视觉验收”说明，已删除该遗留指令式文案；最终 run `f7f2c8c0-0516-479d-a03a-794f6e0b6f8b` 返回“图片已生成（1632×2048），可以继续提出修改。”，同 job，没有重复生成。
