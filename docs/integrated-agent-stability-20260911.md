# Agent 稳定性集成修复（2026-09-11）

## 已实施并部署

- 确认语义：TypeScript 和 SQL 同时区分确认、取消、问句、回应及新修改；自然确认沿用当前方案，真正修改不复用旧授权。
- 审核截断：仅截断触发一次有界输出预算恢复，仍受供应商上限、次数和总超时限制，不绕过审核。
- 失败回执：明确本次调用未执行，不把内部错误说成需求不清，也不删除旧方案或擅自恢复过时确认。
- 取消边界：备用供应商、后处理和远程分层实际推理前重新读取取消状态；不承诺撤回供应商已经接受的请求。
- Skills：当前阶段完整指南以非授权的方法数据注入，去重、替换并参与预算；压缩保留当前指南，能力不匹配或过大不得假称加载成功。业务领域优先，明确检索需求使用提示库，简单修改不强制重复检索。
- 关闭遗留无人值守 RPC 的执行权限，不删除历史数据，不改变普通对话生成权限。

数据库迁移 20260911000011、20260911000012 已在本地副本提交。API、worker 已在无活动任务时重启。

## 可控验证

最终合并回归：14 个文件，370 项通过。以下较早/分组计数是过程记录，与最终计数重叠，不累加。

- 12 个相关测试文件：337 项通过。
- 最终模型请求集成测试：2 项通过；完整技能正文恰好一份，原用户仍为最后一条用户消息，预算检查有效。
- 服务端 TypeScript 检查通过。
- 真实数据库回滚事务验证：自然修改确认保留方案；问句、否定及真正新增修改不获得生成授权。

## 真实流程验收（本次范围完成）

隔离画布：fb89e5c2-df56-44cf-a8fd-10b697532d5e；会话：da094c50-e6e0-4429-ac56-175825cbbb5f。
证据：artifacts/paid-dialogue-live/qa-integrated-stability-20260911.json。

- 第一轮实际加载 logo-design 2.1.0，并执行提示库搜索及选中条目读取；只保存方案，未生成。
- 第二轮“就按这个方案生成吧”只提交一个任务 7645340c-e3c1-48a0-b7d0-ed9069fb43f8。
- 该任务完成，资产 25205892-0c86-4d44-b3d8-8df2083d45fa，2048×2048；浏览器刷新后图片及输入框可见。
- 第三轮真实修改暴露 `intent_review_unfounded_selector`：代码的最近成图名词识别遗漏 Logo，未提交生图。已统一两处指代校验，并保留单一最近结果、当前原话、同会话/画布及真实资产约束；明确修改允许 edit，纯参考不能误用 edit。相关门禁 129 项通过。
- 第四轮在同一会话重试原话，成功保存修改方案，正确保留 MUNO→MUYE、原图及比例。
- 第五轮“确认，按这次修改生成。”一次提交任务 fb31049a-f555-41fd-ba2d-b09b5e28f43e；已成功生成 2048×2048，资产 88bfbde8-ad76-4db1-b4ef-ab24705a559d。真实携带一张参考图；浏览器可见 MUYE 修改版及保留的 MUNO 原版。不据此承诺像素级无差异。
- 第六轮同品牌春季海报：实际加载 campaign-design（stage=prompt, outputKind=raster-image），保存 4:5、暮野 MUYE、春日新茶、把春天泡进杯里及品牌配色的方案；没有提示库检索，没有新生图任务。
- 会话共 6 轮，其中第 3 轮暴露缺陷，第 4 轮修复后恢复；最终只有 2 个生图任务，均 succeeded。没有把失败轮隐藏或计为通过。
- 浏览器刷新后保留两张画布图片及可用输入框；截图 artifacts/paid-dialogue-browser/fb89e5c2-df56-44cf-a8fd-10b697532d5e-integrated-results.png 已人工查看。
- 同时明确参考图检查应使用 reference_analysis，不得用尚未生成的新要求判旧图失败；该工具 28 项回归通过。

上述后续修复部署前再次通过类型检查，相关门禁/参考检查/技能包三个文件 161 项通过。计数与前面的 337 有重叠，不累加为独立案例总数。

## 验收边界

真实测试使用 DeepSeek 文本模型和第三方 GPT Image 2。可控故障测试不等于真实第三方故障注入；本地 credits 未覆盖非零扣费退款。并发确认、多用户隔离、真实多参考图、长会话重启恢复仍需专项生产回归，不能据此声称无限轮无错误。

实际分工：两个 Sol/high 子任务分别负责确认语义及 Skills/上下文；主控完成审核恢复、取消检查、权限清理、部署与集成验收。
